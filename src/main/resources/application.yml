server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  mail:
    host: localhost
    port: 1025
    username: steve
    password: steve

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
